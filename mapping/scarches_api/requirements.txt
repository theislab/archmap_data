# for scarches
scanpy==1.12
xgboost==3.2.0
numpy==2.4.3
# pandas 3.0 changed .astype(str) to leave real NaNs in place of the string "nan"
# (future.infer_string default), which breaks anndata's h5ad string writer during
# concat_on_disk (TypeError: Can't implicitly convert non-string objects to strings).
# Keep on the pandas 2.x line until the writer path is confirmed compatible.
pandas<3
torch~=2.10.0
scikit-misc==0.5.2
protobuf==7.34.1
pymde==0.3.0
scikit-learn==1.8.0
scvi-tools==1.4.2
scipy>=1.17.1


#concatenation on disk
dask
#anndata==0.1

# for the api
Flask==2.1.1
Werkzeug==2.2.2
gunicorn==20.1.0

# for s3
boto3==1.22.1

#for uncertainty
#pertpy instead installed with git for new features (rpy2 error)
# pertpy

#scvi-hub
huggingface_hub