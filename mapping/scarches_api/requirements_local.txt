# for scarches
scanpy==1.12
xgboost==3.2.0
numpy==2.4.3
# pandas 3.0 changed .astype(str) to leave real NaNs in place of the string "nan"
# (future.infer_string default), which breaks anndata's h5ad string writer during
# concat_on_disk (TypeError: Can't implicitly convert non-string objects to strings).
# Keep on the pandas 2.x line until the writer path is confirmed compatible.
pandas<3
torch~=2.10.0
#jaxlib==0.3.7
#leidenalg==0.8.9
scikit-misc==0.5.2
protobuf==7.34.1
pymde==0.3.0
scikit-learn==1.8.0
scvi-tools==1.4.2
scipy>=1.17.1


#concatenation on disk
dask==2026.3.0
# anndata >=0.13 makes `.X` addressable as `layers[None]`, which breaks the
# zero-padding AnnData construction in scvi-tools <1.4.3 and in the pinned scarches
# fork (ValueError: If you provide `layers[None]` and `X`, they must be identical).
# Left unpinned deliberately: scarches_api/utils/compat.py patches around it.


# for s3
boto3

#for uncertainty
#pertpy instead installed with git for new features (rpy2 error)
# pertpy
